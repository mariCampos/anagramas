
module.exports = {
    formaAnagrama(palavra) {
        var letras = palavra.split('');
        var resultado = '';

        for(var i = 0; i < letras.length; i++) {
            
        }
        return resultado;
    }
}